spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

  ai:
    openai:
      api-key: "test-placeholder-not-real"
      base-url: https://generativelanguage.googleapis.com/v1beta/openai
      chat:
        options:
          model: gemini-2.0-flash
